# Online-Quiz-Maker
Building a quiz platform transitions you into Full-Stack development. While HTML/CSS handles the UI, JavaScript (or a framework like React) manages the "flow"—switching from a Quiz Listing to an active session where questions appear one at a time. The core challenge is data handling. 
